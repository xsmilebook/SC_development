# 会话记录（2026-02-11）

## 目标
- 为 ABCD 的 ComBat-GAM 新增 neuroHarmonize（与 HCP-D 一致的 mgcv 基函数）版本。
- 覆盖 cognition、pfactor、baseline age/sex/meanFD、CBCL、age-corrected cognition 与 fully-corrected cognition 变体。
- 提供一键 sbatch 提交脚本，输出统一写入 `outputs/results/combat_gam/abcd/baseline/`。

## 主要新增
- neuroHarmonize 变体脚本：`combat_gam/scripts/run_abcd_combat_gam_neuroharmonize.py`
  - 变体：`cognition`（baseline-only）、`pfactor`（非 baseline）、`baseline_age_sex_meanfd`（baseline-only）、`cbcl`、`comp_agecorrected_baseline`（baseline-only）、`fluidcomp_fc_baseline`（baseline-only）。
  - 输入默认：`/ibmgpfs/cuizaixu_lab/xuxiaoyu/SC_development/interdataFolder_ABCD/SCdata_SA12_CV75_sumSCinvnode.sum.msmtcsd.merge.rds`。
  - 输出命名：`*combatgam_neuroharmonize_<variant>.rds`。
  - 缺失表型列时按 `scanID` 从 `demopath/DemodfScreenFinal.csv` 回填（仅限 `cbcl` 与 cognition 相关列）。
- 一键运行脚本：`combat_gam/scripts/run_abcd_combat_gam_neuroharmonize_baseline.sh`
- sbatch 提交脚本：`combat_gam/sbatch/abcd_combat_gam_neuroharmonize_baseline.sbatch`

## 主要调整
- ABCD cognition 相关脚本（`development_script/5th_cognition/`）输入切换为 neuroHarmonize baseline 输出（`outputs/results/combat_gam/abcd/baseline/*combatgam_neuroharmonize_*.rds`）。
- 同步更新 `docs/workflow.md` 中 cognition 相关入口与输入说明。

## 输出位置
- `outputs/results/combat_gam/abcd/baseline/`

## 运行方式（用户在集群上提交）
- `sbatch combat_gam/sbatch/abcd_combat_gam_neuroharmonize_baseline.sbatch`
