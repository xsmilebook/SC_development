# 会话记录（2026-02-04）

## 目标
- 新增 age_wp/age_bp LMM 分析脚本（SC、cognition、pfactor 三版本）。
- 统一使用可复用的 parametric-bootstrap ANOVA 函数并生成所需矩阵/散点/柱状图。

## 变更
- 新增函数：`gamfunction/pb_lmm_anova.R`（full vs null 的 parametric-bootstrap ANOVA）。
- 新增脚本：
  - `development_script/2nd_fitdevelopmentalmodel/run_abcd_lmm_agewp_agebp_SC.R`
  - `development_script/5th_cognition/run_abcd_lmm_agewp_agebp_cognition_groups.R`
  - `development_script/6th_pfactor/run_abcd_lmm_agewp_agebp_pfactor_general.R`
- 三版本均拟合：`SC ~ age_wp + age_bp + sex + mean_fd + (1 + age_wp || subID)`；
  - age_wp = age_ij - mean(age)_i
  - age_bp = mean(age)_i
- SC 版本保留单时间点被试；cognition/pfactor 版本过滤单时间点被试。
- 输出：age_bp partial R² 矩阵（FDR 标注）与 S-A 相关散点；
  cognition/pfactor 额外输出 personal slope 低/高组 t 值矩阵（FDR 标注）、decile 柱状图、以及与 cognition/pfactor 的相关矩阵与 S-A 相关散点。

## 文档更新
- `docs/workflow.md`：新增 age_wp/age_bp LMM 三版本入口与输出说明。
- `PROGRESS.md`：记录新脚本与函数文件。

## 备注
- 本次未提交集群作业；脚本可在本地或集群环境按需运行。
