# 会话记录（2026-02-08）

## 目标
- 按用户要求关闭 decile 图中的显著性结果显示（不在图上标注 p/FDR）。

## 代码变更
- 修改 `development_script/5th_cognition/run_abcd_lmm_agewp_agebp_baselineage_cognition_interaction_decileavg.R`：
  - 在 `decile_avg_sc_first` 两处绘图调用中移除 `p_col` 与 `p_df` 参数。
- 修改 `development_script/6th_pfactor/run_abcd_lmm_agewp_agebp_baselineage_pfactor_interaction_decileavg.R`：
  - 在 `decile_avg_sc_first` 两处绘图调用中移除 `p_col` 与 `p_df` 参数。

## 行为说明
- 图像：不再显示 FDR p 值文本。
- 统计：LRT 与 FDR 计算逻辑保持不变，`decile_avgSC_summary_baselineage_interaction_*.csv` 仍输出显著性结果。

## 文档更新
- 更新 `docs/workflow.md`、`development_script/5th_cognition/README.md`、`development_script/6th_pfactor/README.md`，同步“图中不显示显著性文本”的说明。
- 更新 `PROGRESS.md` 增加本次记录。

## 验证
- 本地环境无 `Rscript`，未执行运行级验证；已完成代码与调用参数静态核对。

## 追加需求（decile 图标注 t 与 p）
- 用户要求在 10 张 decile figure 上标注 `T value` 和 `p value`。
- 已在 cognition 与 pfactor 的 baseline-age decile 脚本中实现：
  - `t`：交互项系数的 t 值（`age_wp:cov` 或 `age_bp:cov`）。
  - `p`：`red vs full` 的 LRT p 值（与脚本内显著性检验保持一致）。
- 标注位置：`decile_avg_sc_first` 的 decile 图（每个 decile 一条图注，格式 `t=..., p=...`）。
- FDR 列（`p_agewp_cov_fdr`、`p_agebp_cov_fdr`）仍继续输出到 summary CSV，不受影响。

## 追加需求（提取 10 个 decile 的 age_wp 项 p 值）
- 用户要求提取并输出 10 个 decile 的 `age_wp` 项 p 值。
- 已在 cognition 与 pfactor 脚本中新增：
  - `p_agewp_main`：`full(y ~ age_wp + age_bp*cov + sex + mean_fd + (1|subID))` 对比
    `red(y ~ age_bp*cov + sex + mean_fd + (1|subID))` 的 LRT p 值；
  - `p_agewp_main_fdr`：对上述 10 个 decile 的 FDR 校正。
- 输出文件：
  - `decile_agewp_pvalues_baselineage_nihtbx_fluidcomp_uncorrected_base_CV75.csv`
  - `decile_agewp_pvalues_baselineage_GENERAL_base_CV75.csv`
- 两文件均包含 3 列：`decile`, `p_agewp_main`, `p_agewp_main_fdr`。
