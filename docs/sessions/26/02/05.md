# 会话记录（2026-02-05）

## 目标
- 更新 age_wp/age_bp SC 脚本：新增 age_wp 固定效应（beta）的矩阵与 S-A 相关散点图。
- 调整 partial R^2 的 3-SD 过滤逻辑：仅在散点图阶段过滤，不影响矩阵图。
- 若已有结果文件，脚本默认直接读取并跳过重新拟合。
- 在 `development_script/2nd_fitdevelopmentalmodel` 新增 SC personal slope（LGCM-style slope_per_year）的分析脚本：输出 personal slope 均值矩阵与 S-A 相关散点图（不标注显著性）。
- 将 SC personal slope 改为在被试层面控制 `age_t0/sex/mean_fd_t0/mean_fd_t1` 后的残差均值作为 edge 指标，并据此绘图。
- 修正：含截距的线性模型中残差均值恒为 0；因此对数值协变量做中心化、`sex` 使用 sum contrasts，并以模型截距作为 covariate-adjusted mean slope（用于矩阵与 SA 轴相关散点图）。

## 变更
- `development_script/2nd_fitdevelopmentalmodel/run_abcd_lmm_agewp_agebp_SC.R`：
  - 新增 `FORCE=1` 开关；默认若结果 `*.rds` 已存在则直接读取。
  - age_bp partial R^2：矩阵使用原值；散点图使用 3-SD 过滤后的值。
  - age_wp/age_bp partial R^2：矩阵使用原值；散点图使用 3-SD 过滤后的值。
  - 新增 age_wp/age_bp fixed effect（beta）矩阵与 S-A 相关散点图。

## 文档更新
- `docs/workflow.md`：补充 age_wp fixed effect 输出与 3-SD 过滤仅用于散点图。
- `docs/workflow.md`：新增 SC personal slope（LGCM-style slope_per_year）脚本说明与输出路径。
- `PROGRESS.md`：记录本次更新。
