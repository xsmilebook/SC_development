# 2026-01-16 会话记录

- 需求：将 AGENTS.md 改为中文并移除根目录英文规则，只保留文件名英文。
- 变更：重写 `AGENTS.md` 为中文版本，更新文档语言约定。
- 记录：新增 `PROGRESS.md` 并登记本次变更。
- 待办：无。
- 追加：新增 `docs/README.md`、`docs/workflow.md`、`docs/methods.md`，汇总方法学与流程说明。
- 追加：更新 `README.md` 文档入口，并调整 `AGENTS.md` 路径约定以支持绝对路径。
- 追加：基于 `docs/research` 中的论文与补充材料，扩展 `docs/methods.md` 的样本与方法学要点。
- 追加：更新 `ARCHITECTURE.md` 与 `docs/workflow.md`，匹配现有结构与绝对路径使用规范。
- 追加：更新 `AGENTS.md`、`ARCHITECTURE.md` 与 `docs/workflow.md`，明确 `data/` 与 `outputs/` 的使用规则并保持 `wd/` 不变。
- 追加：新增 `docs/combat_plan.md`，记录 ComBat/ComBat-GAM 实现计划与协变量配置。
- 追加：修订 `docs/combat_plan.md` 的 ABCD 协变量方案与纵向合并策略，并对齐 GAMM 设定。
- 追加：确认沿用既有数据，仅修改方法；GAMM 调用位置与既有脚本一致。
- 追加：更新 `docs/combat_plan.md`，ABCD 协变量加入 meanFD，并补充数据路径清单占位。
- 追加：补齐 HCP-D、ABCD 与 Chinese Cohort 的集群路径。
- 追加：补充 ComBat 使用的 SC 数据与协变量表路径。
- 追加：明确 ABCD 认知方案沿用基线筛选规则。
- 追加：在 `AGENTS.md` 增加“未经指示不写入项目路径外文件”的约束。
- 追加：新增 `combat_gam/` 并整理 neuroHarmonize 与纵向 ComBat 代码。
- 追加：在 `AGENTS.md` 流程中新增“修改代码或产生新结果需补充文档说明”的要求。
- 追加：更新 `AGENTS.md` 的执行规则（小型任务直跑，正式任务 sbatch/q_fat_c）。
- 追加：同步 `PLAN.md` 执行规则（测试直跑，正式任务 sbatch/q_fat_c）。
- 追加：新增 ComBat-GAM 脚本与 sbatch 模板，并完成三套数据的小样本测试。
